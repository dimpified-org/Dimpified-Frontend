import React from "react";
import MinimalistTemplate from "../../PaidTemplates/MinimalistTemplate";

const Template52 = ({ details, subdomain, userDetails }) => {
  return (
    <MinimalistTemplate subdomain={subdomain} userDetails={userDetails} />
  );
};

export default Template52;
